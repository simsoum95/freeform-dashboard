# docs

Record a short screen capture of the freeform editor (drag/resize/style/scenes)
and save it here as `demo.gif`. The README and the HACS plugin page both show it,
and a GIF is what actually drives adoption for a visual tool like this.
